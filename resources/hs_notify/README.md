# hs_notify

A clean standalone notification system for FiveM.

## Features

- Standalone
- Client export
- Client event
- Server-to-client event
- Configurable notification types
- Simple debug logging

## Installation

Place the resource in your server resources folder and add this to `server.cfg`:

```cfg
ensure hs_notify
```
